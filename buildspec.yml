version: 0.2

phases:
  pre_build:
    commands:
      - npm install --force
      - npm install @nestjs/cli@latest -g
  build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'